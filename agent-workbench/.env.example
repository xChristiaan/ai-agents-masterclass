# Rename this file to .env and fill in your credentials.
# Only OPENAI_API_KEY is required for the default configuration.

OPENAI_API_KEY=""
# Optional: override the API base URL for OpenAI-compatible providers
OPENAI_BASE_URL=""
# Optional: use Azure OpenAI by specifying deployment information
AZURE_OPENAI_API_KEY=""
AZURE_OPENAI_ENDPOINT=""
AZURE_OPENAI_DEPLOYMENT=""
# Optional: override the Azure API version (defaults to 2023-05-15)
AZURE_OPENAI_API_VERSION=""
# Optional: connect to a remote Ollama host for local models
OLLAMA_HOST=""
